# Server
PORT=3001
NODE_ENV=development

# Database (MySQL) – required for the backend to work
# Without a running MySQL and these vars, you'll see "connect ECONNREFUSED 127.0.0.1:3306"
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=tycoon
DB_USER=root
DB_PASSWORD=your_password

# Optional: skip Redis (no cache, no Redis process needed)
SKIP_REDIS=true

# Optional: Redis (when not using SKIP_REDIS)
# REDIS_URL=redis://127.0.0.1:6379

# --- Celo / Tycoon contract (backend game controller) ---
# Required for setTurnCount, removePlayerFromGame, transferPropertyOwnership.
# Set the wallet as backendGameController on the Tycoon contract (owner calls setBackendGameController).
CELO_RPC_URL=https://rpc.ankr.com/celo
# CELO_CHAIN_ID=42220 (mainnet) or 44787 (Alfajores testnet). Default 42220.
TYCOON_CELO_CONTRACT_ADDRESS=
# Paste your game controller wallet private key below (0x...). Keep secret; never commit.
BACKEND_GAME_CONTROLLER_PRIVATE_KEY=

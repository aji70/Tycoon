import AdminSectionPlaceholder from "@/components/admin/dashboard/AdminSectionPlaceholder";

export default function AdminModerationPage() {
  return (
    <AdminSectionPlaceholder
      title="Moderation"
      description="Reports queue, chat moderation, and fraud signals — needs reports schema and workflows."
    />
  );
}

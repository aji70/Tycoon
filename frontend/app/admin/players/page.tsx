import AdminSectionPlaceholder from "@/components/admin/dashboard/AdminSectionPlaceholder";

export default function AdminPlayersPage() {
  return (
    <AdminSectionPlaceholder
      title="Player management"
      description="Search, profiles, suspend/ban, and reward adjustments will connect to /api/admin/players in a following step."
    />
  );
}

import AdminSectionPlaceholder from "@/components/admin/dashboard/AdminSectionPlaceholder";

export default function AdminAnalyticsPage() {
  return (
    <AdminSectionPlaceholder
      title="Analytics & reports"
      description="Funnels, retention, revenue, and technical metrics — can reuse /api/analytics with admin auth."
    />
  );
}

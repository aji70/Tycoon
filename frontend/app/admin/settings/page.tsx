import AdminSectionPlaceholder from "@/components/admin/dashboard/AdminSectionPlaceholder";

export default function AdminSettingsPage() {
  return (
    <AdminSectionPlaceholder
      title="System settings"
      description="Dice odds, entry fee, max players, reward multiplier, maintenance mode, and health checks."
    />
  );
}

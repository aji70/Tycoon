import AdminSectionPlaceholder from "@/components/admin/dashboard/AdminSectionPlaceholder";

export default function AdminWalletMonitorPage() {
  return (
    <AdminSectionPlaceholder
      title="Wallet & blockchain monitor"
      description="Connected wallets, token transfers, and NFT mints via indexer or subgraph."
    />
  );
}

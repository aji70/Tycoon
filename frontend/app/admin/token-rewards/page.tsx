import AdminSectionPlaceholder from "@/components/admin/dashboard/AdminSectionPlaceholder";

export default function AdminTokenRewardsPage() {
  return (
    <AdminSectionPlaceholder
      title="Token & reward management"
      description="Economy config, manual grants, and burn/rollback will extend the existing rewards stack."
    />
  );
}

import AdminSectionPlaceholder from "@/components/admin/dashboard/AdminSectionPlaceholder";

export default function AdminLeaderboardPage() {
  return (
    <AdminSectionPlaceholder
      title="Leaderboard management"
      description="Daily / weekly / monthly / all-time tabs and admin actions (reset, exclude accounts)."
    />
  );
}
